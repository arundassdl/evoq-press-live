[servers]
188.245.156.163 ansible_user=frappe ansible_python_interpreter=/usr/bin/python3 server=evoq.app private_ip=188.245.156.163

[servers:vars]
# Agent configuration
agent_password=
monitoring_password=
workers=2

# Certificate configuration (leave empty to auto-generate self-signed)
certificate_private_key=
certificate_full_chain=
certificate_intermediate_chain=

# Optional: Agent repository settings
# agent_repository_url=https://github.com/frappe/agent.git
# agent_branch=master

# Optional: Proxy settings (if behind a proxy)
# proxy_ip=

